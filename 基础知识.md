# switch语句：  

## 1.例子：switch（变量）：  
{  
    case 常量1：  
        语句1  
        break  
    case 常量2：  
    语句2  
    break  
}  
# lambda 函数  
## 1.例子：  
函数名字=lambda 变量：表达式  
# 数学函数  
## 1.头文件  
#include <cmath(1)>  
## 2.常见函数  
cos(弧度角)  
sin(弧度角)  
log（double）  
pow（底数，指数）  
floor（） 返回一个数取下值  
sqrt（double）  
abs（int）  
fabs（double）  
## 3.随机数  
srand（种子数）  
f=rand（）  
# 字符串函数  
## 1.头文件 
#Include<cstring(1)>    
## 2.常用函数 
strcpy：char a[13]="a,b,c,d";char c[13];strcpy(c,a);  
strcat: char a[13],c[13];strcat(a,c)  
strlen: l=strlen(a)  
strcmp  


